apiVersion: v1
kind: ResourceQuata
metadata:
  name: compute-quata
  namespace: default

spec:
  hard:
    pods: "10"
    requests.cpu: "4"
    requests.memory: 5Gi
    limits.cpu: "10"
    limits.memory: 10Gi
